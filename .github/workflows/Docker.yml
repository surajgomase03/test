name: Docker

on: workflow_dispatch

jobs:
    Build:
        runs-on: ubuntu-latest
        steps: 
          - name: Checkout
            uses: actions/checkout@v4
        
          - name: Setup node and dependecies
            uses: ./.github/actions
          