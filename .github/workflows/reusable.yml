name: reusable

on: 
  workflow_call:
    inputs:
      artifact-name:
        default: suraj
        required: false
        type: string

jobs:
    Show:
      runs-on: ubuntu-latest
      steps:
        - name: Output
          run: echo " call this yml from another actions"

        - name: Show Output
          uses: actions/download-artifact@v4
          with:
            name: ${{ inputs.artifact-name }}
        - name: list object
          run: ls -lrt
          

          