const test = require('node:test');
const assert = require('node:assert');

test('health check sanity', () => {
  assert.strictEqual(1 + 1, 2); // placeholder — replace with real supertest later if you want
});